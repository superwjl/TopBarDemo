<com.tik.testa1.custom.TopBar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/topbar"
    android:layout_width="match_parent"
    android:layout_height="45dp"
    app:leftTextColor="@android:color/white"
    app:leftTextSize="14dp"
    app:leftBackground="@null"
    app:leftImageSrc="@null"
    app:leftText="返回"
    app:rightTextColor="@android:color/white"
    app:rightTextSize="14dp"
    app:rightBackground="@null"
    app:rightImageSrc="@null"
    app:rightText="操作"
    app:titleTextColor="@android:color/white"
    app:titleTextSize="16dp"
    app:title="标题"
    android:background="@color/colorPrimaryDark"
    />